# Library Management System

Flask + React library management app with member and admin roles.

## Setup

### Backend

```
cd backend
pip install -r requirements.txt
python app.py
```

### Frontend

```
cd frontend
npm install
npm start
```

## Default Accounts

- Admin: `admin` / `admin123`
- Member: `member` / `member123`
